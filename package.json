{
  "name": "ProjectServer",
  "version": "1.0.0",
  "description": "Zebra",
  "scripts": {
    "start": "npm -install discord.js -- node bot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}